resources:
  jobs:
    silver_dedup_trips:
      name: silver_dedup_trips
      tasks:
        - task_key: dedup_trips
          spark_python_task:
            python_file: ../src/silver/dedup_trips.py
          environment_key: default
      environments:
        - environment_key: default
          spec:
            environment_version: "2"
