resources:
  jobs:
    gold_daily_revenue_table:
      name: gold_daily_revenue_table
      tasks:
        - task_key: build_daily_revenue
          spark_python_task:
            python_file: ../src/gold/daily_revenue_table.py
          environment_key: default
      environments:
        - environment_key: default
          spec:
            environment_version: "2"
